services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: lems-api
    restart: unless-stopped
    env_file: .env
    network_mode: host
    
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: lems-cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TOKEN}
    depends_on:
      - api
    network_mode: host